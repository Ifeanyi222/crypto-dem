PORT=4000
JWT_SECRET=bc5a89feaba3349dd89a88d08a56be896cc5e3def0e87e3ed5af499f823b5c07
APP_URL=http://localhost:5173

# Optional: leave blank to just log reset links to the console during development.
# Fill these in with a real provider (SendGrid, Mailgun, AWS SES, Gmail SMTP, etc.) to send real emails.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Vaultra <no-reply@vaultra.example>"

# Fiat on-ramp (buy crypto with card/bank transfer) sign up free at https://dashboard.transak.com
# Leave blank until you have a Transak partner account; the Fund Wallet button will show
# a clear "not configured" message instead of failing silently.
TRANSAK_API_KEY=
TRANSAK_ACCESS_TOKEN=

# Real on-chain trading via 0x's DEX aggregator sign up free at https://dashboard.0x.org
# Leave blank until configured; the trading panel will show a clear "not configured" message.
ZEROX_API_KEY=c3c6fa03-880c-4dd0-b921-16ce4da60b64
